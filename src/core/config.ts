class Config {
    configPath = '';
    /**
     * 从文档读取配置
     * @param path 
     */
    readConfigFromFile(path: string) {
    }
    
    /**
     * 更新配置到文档
     * @param path 
     * @param configs 
     */
    updateConfigFromFile(path: string, configs: string) {
    }
}